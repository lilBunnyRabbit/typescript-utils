/**
 * This `core` module acts as the foundation of the package, providing essential utilities, foundational classes,
 * and common functionalities that are leveraged across the entire package.
 * It's designed to offer the basic building blocks and shared resources that other modules within the package depend upon
 * for their operation.
 *
 * ## Importing
 *
 * ```ts
 * import * as Core from "@lilbunnyrabbit/utils/core";
 * ```
 *
 * @packageDocumentation
 */

export * from "./type-guards";
export type * from "./types";
